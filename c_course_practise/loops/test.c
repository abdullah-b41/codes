#include <stdio.h>
#include <math.h>

int main()
{
    // int n;
    // scanf("%d", &n);
    // double total = 0;
    // for (int i = 1; i <= n; i++)
    // {
    //     double num = 1, den = 1;
    //     num = pow(2 * i - 1, i - 1);
    //     for (int j = 1; j <= i; j++)
    //     {
    //         den *= j;
    //     }
    //     total += num / den;
    // }
    // printf("%lf", total);

    int i = 2, j = 2;
    while (i + 1 ? --i : j++)
        printf("%d", i);
    return 0;
}