#include <bits/stdc++.h>
using namespace std;

int main()
{
    ios::sync_with_stdio(0);
    cin.tie(0);
    cout.tie(0);
    string s;
    cin >> s;
    int n = s.length();
    string ans;
    strcpy(ans, s);
    for (int i = 0; i < 2 * n; i++)
    {
        if(i%2=0)
        {
            ans[i]=s[i]
        }
        
    }
}