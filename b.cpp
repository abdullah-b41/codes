#include <bits/stdc++.h>
using namespace std;

void solve()
{
    string s;
    cin >> s;
    vector<int> kitchen(3), price(3);
    for (auto &it : kitchen)
        cin >> it;
    for (auto &it : price)
        cin >> it;
    long long r, burger = 0;
    cin >> r;
}

int main()
{
    ios_base::sync_with_stdio(false);
    cin.tie(NULL);
    int t = 1;
    while (t--)
    {
        solve();
    }
}
