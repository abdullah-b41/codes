#include <stdio.h>

int main()
{
    int n;
    scanf("%d", &n);

    int old[n];
    for (int i = 0; i < t; i++)
    {
        scanf("%d", &old[i]);
    }

    int cnt = 0, new[n];
    for (int i = 0; i < n; i++)
    {
        int x = old[i];
        for(int j=0;j<n;j++)
        {
            if()
        }
    }
}