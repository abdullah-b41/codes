#include <stdio.h>

int main()
{
    int t;
    scanf("%d", &t);
    while (t--)
    {
        int n, arr[300000];
        scanf("%d", &n);
        for (int i = 0; i < 3 * n; i++)
        {
            if(i%3==0)
        }
    }
}