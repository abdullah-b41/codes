#include <stdio.h>

int main()
{
    int t;
    scanf("%d", &t);
    while (t--)
    {
        int n, m, x1, y1, x2, y2;
        scanf("%d %d", &n, &m);
        scanf("%d %d %d %d", &x1, &y1, &x2, &y2);
        for (int i = 0; i < n; i++)
        {
        }
    }
}