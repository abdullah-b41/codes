#include <bits/stdc++.h>
using namespace std;

int main()
{
    int n, input[1000000] = {}, output[1000000] = {};
    cin >> n;
    for (int i = 0; i < n; i++)
    {
        cin >> input[i];
    }
    int mex = 0;
    for (int i = 0; i < n; i++)
    {
        for (int j = 0; j <= i + 1; j++)
        {
            input[j];
        }
    }
}